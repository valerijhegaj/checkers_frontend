import {useHistory} from "react-router-dom";
import {Ways} from "./ways/ways";

const Authorization = () => {
  return (
    <div>
      <button onClick={() => {}}>
        login
      </button>
      <button onClick={() => {}}>
        register
      </button>
    </div>
  )
}

export default Authorization